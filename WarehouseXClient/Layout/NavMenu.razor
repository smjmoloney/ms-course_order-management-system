<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">WarehouseXClient</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="benchmark">
                <span class="bi bi-speedometer2" aria-hidden="true"></span> Query Benchmark
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="code-benchmark">
                <span class="bi bi-code-slash" aria-hidden="true"></span> Code Benchmark
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="error-handling">
                <span class="bi bi-shield-exclamation" aria-hidden="true"></span> Error Handling
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
